# Tools & Data Specialist Context — Current Stage

## Objective
Provide small deterministic tools and reproducible evaluation data.

## Current tools
- calculator
- get_current_date

## Tool safety
The calculator uses AST parsing with an explicit operator allow-list and bounded inputs/results. No `eval()`.

## Tool contract
`contracts/tools_contract.json`

## Agent integration
`tools/tool_implementation.py` exposes:
- `TOOL_FUNCTIONS` — application allow-list
- `TOOL_DECLARATIONS` — Gemini function schemas

The model requests tools; the application executes them.

## Data
Keep sample technical documents and evaluation data small, realistic and version-controlled.

## Tests
Include valid inputs, malformed inputs, dangerous expressions, and deterministic edge cases.

## Do not
- add tools merely for novelty
- place network calls in deterministic utility tools unless explicitly required
